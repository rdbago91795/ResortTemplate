/*
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self' data: blob: https://eukapgrafolpwgdwyils.supabase.co; font-src 'self'; connect-src 'self' https://eukapgrafolpwgdwyils.supabase.co wss://eukapgrafolpwgdwyils.supabase.co; frame-ancestors 'none'; form-action 'self'; base-uri 'none'; object-src 'none'; upgrade-insecure-requests
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), camera=(), microphone=(), payment=()

# ─────────────────────────────────────────────────────────────────────────────
# Production security headers. Netlify/Cloudflare Pages `_headers` format.
#
# ⚠ INCOMPLETE — the host is not yet chosen, and this file only takes effect on hosts that
# read `_headers`. On Vercel use `vercel.json`; on a Node host set them at the server.
#
# ✅ RESOLVED — the Supabase origin is now in `connect-src` and `img-src`. `wss:` is listed
#   separately because Realtime opens a WebSocket, and `connect-src` does not infer the ws
#   scheme from the https one.
#
#   ⚠ THIS ORIGIN IS PER-DEPLOYMENT. The project ref below belongs to the Balai Amihan
#   development project. Every client deployment gets its own Supabase project and MUST have
#   this line changed to match, or the site loads and then fails every request with a CSP
#   error that looks like a network outage.
#
# ⚠ TURNSTILE, still outstanding — added when the booking forms arrive (US1/US2, T081):
#     script-src   ... https://challenges.cloudflare.com
#     frame-src    https://challenges.cloudflare.com
#
# `script-src 'self'` carries no 'unsafe-inline' and no hash allowance, and must stay that
# way — in a client-only SPA the Supabase session lives in localStorage, so CSP is the
# session-security control rather than a hardening nicety (security-baseline.md §2.3).
#
# `style-src 'self'` is achievable because Vanilla Extract compiles to static CSS at build
# time. A runtime CSS-in-JS library would force 'unsafe-inline' here.
# ─────────────────────────────────────────────────────────────────────────────
